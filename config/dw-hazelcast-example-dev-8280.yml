server:
  applicationConnectors:
    - type: http
      port: 8280
  adminConnectors:
    - type: http
      port: 8281

hazelcastSessionConfig:
  sessionTimeoutSecs: 10
  cookieName: example-session

views:
  .ftl:
    strict_syntax: yes

logging:
  level: INFO
#  loggers:
#    com.hazelcast: ALL